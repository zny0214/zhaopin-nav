# 灵境科技公司主页项目 - 技术文档

**项目版本**：1.0.0
**最后更新**：2026-04-15
**技术栈**：Vue 3 + Vite + Vue Router 4

---

## 一、项目概述

这是一个基于 **Vue 3 + Vite** 技术栈构建的高端科技公司官网，采用 **Composition API**（`<script setup>`）开发模式。

### 1.1 项目定位

- **公司名称**：灵境科技
- **项目类型**：企业官方网站（SPA单页应用）
- **设计风格**：高端精致（Premium）、极简主义、大留白
- **目标用户**：企业客户、潜在合作伙伴、技术求职者

### 1.2 核心技术

| 技术 | 版本 | 用途 |
|:-----|:-----|:-----|
| Vue | 3.4.0 | 核心框架 |
| Vue Router | 4.6.4 | SPA路由管理 |
| Vite | 5.0.0 | 构建工具 |
| @vitejs/plugin-vue | 5.0.0 | Vue SFC支持 |

### 1.3 字体配置

| 字体类型 | 字体名称 | 用途 |
|:---------|:---------|:-----|
| 标题字体 | Cormorant Garamond | 衬线字体，高端典雅 |
| 正文字体 | Noto Sans SC | 思源黑体，无衬线 |

---

## 二、页面路由架构

本项目共有 **11个页面**，采用Vue Router进行SPA导航。

### 2.1 路由配置

| 路径 | 页面组件 | 页面名称 | 功能描述 |
|:-----|:---------|:---------|:---------|
| `/` | Home.vue | 首页 | 公司概览、服务入口 |
| `/products` | Products.vue | 产品中心 | 产品列表展示 |
| `/cases` | Cases.vue | 案例展示 | 行业案例筛选 |
| `/services/ai-solution` | AISolution.vue | AI解决方案 | 人工智能详情页 |
| `/services/cloud-platform` | CloudPlatform.vue | 云计算平台 | 云计算详情页 |
| `/services/data-analytics` | DataAnalytics.vue | 数据分析 | 大数据详情页 |
| `/services/digital-consulting` | DigitalConsulting.vue | 数字化咨询 | 转型咨询详情页 |
| `/help` | Help.vue | 帮助中心 | FAQ问答 |
| `/terms` | Terms.vue | 服务条款 | 法律条款 |
| `/privacy` | Privacy.vue | 隐私政策 | 隐私保护 |
| `/contact` | Contact.vue | 联系方式 | 在线咨询表单 |

### 2.2 路由特性

```javascript
// 路由配置
scrollBehavior(to, from, savedPosition) {
  if (savedPosition) {
    return savedPosition
  } else {
    return { top: 0 }
  }
}
```

---

## 三、设计系统

### 3.1 主题色彩体系

每个服务详情页拥有独立的主题色彩，强化品牌识别度：

| 服务页面 | 主题色 | 色值 | 应用场景 |
|:---------|:-------|:-----|:---------|
| AI解决方案 | 金色 | `#c9a962` | 按钮、图标、强调 |
| 云计算平台 | 蓝色 | `#5a8fcd` | 按钮、图标、强调 |
| 数据分析 | 绿色 | `#6bab90` | 按钮、图标、强调 |
| 数字化咨询 | 橙色 | `#c97a62` | 按钮、图标、强调 |

### 3.2 通用色彩

| 用途 | 色值 |
|:-----|:-----|
| 深色背景 | `#1a1a1a` |
| 页面背景 | `#faf9f7` |
| 卡片背景 | `#ffffff` |
| 边框色 | `#e8e6e3` |
| 正文文字 | `#1a1a1a` |
| 次要文字 | `#666666` |
| 辅助文字 | `#999999` |

### 3.3 间距系统

采用8px基准的间距体系：

| 命名 | 数值 |
|:-----|:-----|
| `--space-xs` | 4px |
| `--space-sm` | 8px |
| `--space-md` | 16px |
| `--space-lg` | 24px |
| `--space-xl` | 32px |
| `--space-2xl` | 48px |
| `--space-3xl` | 64px |
| `--space-4xl` | 96px |

### 3.4 动画系统

```css
/* 缓动函数 */
--ease-out-quart: cubic-bezier(0.25, 1, 0.5, 1);

/* 过渡时间 */
--transition-fast: 0.2s ease;
--transition-base: 0.3s ease;
```

---

## 四、页面功能详解

### 4.1 首页（Home.vue）

**核心功能**：

| 功能 | 描述 | 技术实现 |
|:-----|:-----|:---------|
| 滚动监听 | 导航高亮当前区块 | `window.addEventListener('scroll')` |
| 数字动画 | 统计数据平滑递增 | `requestAnimationFrame` + 缓动函数 |
| 表单提交 | 咨询表单提交反馈 | `isSubmitting` + `isSuccess` 状态 |
| 平滑滚动 | 点击导航滚动到对应区块 | `scrollIntoView({ behavior: 'smooth' })` |

**组件数据**：

```javascript
// 服务项目
const services = [
  { title: '人工智能解决方案', icon: '◈', route: '/services/ai-solution' },
  { title: '云计算平台', icon: '◇', route: '/services/cloud-platform' },
  { title: '数据智能分析', icon: '◉', route: '/services/data-analytics' },
  { title: '数字化转型咨询', icon: '◎', route: '/services/digital-consulting' }
]

// 统计数据
const stats = [
  { value: 200, suffix: '+', label: '服务企业' },
  { value: 98, suffix: '%', label: '客户满意度' },
  { value: 15, suffix: '+', label: '技术专利' },
  { value: 24, suffix: '/7', label: '全天候支持' }
]
```

### 4.2 产品页（Products.vue）

**核心功能**：
- 产品分类筛选（软件产品 / SaaS服务 / 定制开发）

### 4.3 案例页（Cases.vue）

**核心功能**：
- 行业筛选（金融 / 制造 / 零售 / 医疗）

### 4.4 服务详情页（4个）

每个服务详情页均包含以下交互功能：

#### 通用交互

| 功能 | 描述 |
|:-----|:-----|
| 页面加载动画 | 淡入效果 + 元素依次显示 |
| 功能卡片展开 | 点击展开显示详细技术栈标签 |
| 场景卡片点击 | 点击激活并显示描述 |
| 数字指标动画 | 进入视口时依次显示 |

#### 个性化交互

| 页面 | 特有交互 | 描述 |
|:-----|:---------|:-----|
| AI解决方案 | 流程步骤切换 | 四步服务流程可点击切换 |
| 云计算平台 | Tab切换 | 计算/存储/网络/安全四类切换 |
| 数据分析 | 图表选择器 | 5种可视化图表类型切换 |
| 数字化咨询 | 方法选择器 | 4种咨询方式切换 |

#### 功能卡片数据结构

```javascript
const features = [
  {
    title: '功能名称',
    description: '功能描述',
    details: '标签1 | 标签2 | 标签3'  // 展开后显示
  }
]
```

#### 场景卡片数据结构

```javascript
const scenarios = [
  { name: '场景名称', icon: '◈', desc: '场景描述' }
]
```

### 4.5 帮助中心（Help.vue）

**核心功能**：
- FAQ手风琴组件（点击展开/收起答案）

### 4.6 联系方式（Contact.vue）

**核心功能**：

| 功能 | 描述 |
|:-----|:-----|
| 咨询类型选择 | 产品咨询 / 技术支持 / 商务合作 / 其他 |
| 表单验证 | 必填项验证（姓名、邮箱、留言） |
| 提交状态 | 加载中 → 成功提示（5秒后自动关闭） |
| 表单重置 | 成功提交后自动清空表单 |

**表单数据结构**：

```javascript
const formData = ref({
  type: 'consultation',
  name: '',
  email: '',
  phone: '',
  company: '',
  message: ''
})
```

---

## 五、响应式设计

### 5.1 断点配置

| 断点 | 屏幕宽度 | 适配内容 |
|:-----|:---------|:---------|
| 桌面 | ≥1024px | 完整布局 |
| 平板 | 768px - 1023px | 双列变单列 |
| 移动 | < 768px | 堆叠布局 |

### 5.2 媒体查询示例

```css
@media (max-width: 1024px) {
  .scenarios-content { grid-template-columns: 1fr; }
  .metrics-grid { grid-template-columns: repeat(2, 1fr); }
}

@media (max-width: 768px) {
  .header { padding: var(--space-md) var(--space-lg); }
  .hero-actions { flex-direction: column; }
  .features { padding: var(--space-3xl) var(--space-lg); }
}
```

### 5.3 无障碍支持

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

---

## 六、项目结构

```
公司主页项目/
│
├── src/
│   │
│   ├── router/
│   │   └── index.js          # 路由配置（11个路由）
│   │
│   ├── views/
│   │   │
│   │   ├── Home.vue           # 首页
│   │   ├── Products.vue       # 产品中心
│   │   ├── Cases.vue          # 案例展示
│   │   ├── Contact.vue        # 联系方式
│   │   ├── Help.vue           # 帮助中心
│   │   ├── Terms.vue          # 服务条款
│   │   ├── Privacy.vue        # 隐私政策
│   │   │
│   │   └── services/          # 服务详情页（4个）
│   │       ├── AISolution.vue         # AI解决方案
│   │       ├── CloudPlatform.vue      # 云计算平台
│   │       ├── DataAnalytics.vue      # 大数据分析
│   │       └── DigitalConsulting.vue  # 数字化咨询
│   │
│   ├── App.vue                # 根组件（页面过渡动画）
│   └── main.js                # 应用入口
│
├── public/
│   └── vite.svg               # Favicon
│
├── index.html                 # HTML模板
├── package.json               # 项目依赖
├── vite.config.js             # Vite配置
└── dist/                      # 构建输出目录
```

---

## 七、依赖配置

### 7.1 生产依赖

```json
{
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.6.4"
  }
}
```

### 7.2 开发依赖

```json
{
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}
```

---

## 八、运行命令

```bash
# 安装依赖
npm install

# 开发模式（热重载）
npm run dev

# 生产构建
npm run build

# 预览构建结果
npm run preview
```

---

## 九、亮点与创新

### 9.1 技术亮点

| 亮点 | 说明 |
|:-----|:-----|
| Composition API | 使用 `<script setup>` 语法，代码更简洁 |
| 响应式系统 | Vue 3响应式数据，自动追踪依赖 |
| 路由管理 | Vue Router 4，hash模式.history模式 |
| 构建速度 | Vite极速冷启动 + HMR热更新 |

### 9.2 设计亮点

| 亮点 | 说明 |
|:-----|:-----|
| 主题色彩 | 每个服务页独立主题色，强化品牌认知 |
| 渐变背景 | radial-gradient营造高级感 |
| 毛玻璃效果 | backdrop-filter模糊背景 |
| 卡片悬停 | transform + box-shadow微交互 |

### 9.3 交互亮点

| 亮点 | 说明 |
|:-----|:-----|
| 页面过渡 | App.vue中配置淡入淡出动画 |
| 滚动动画 | IntersectionObserver实现数字递增 |
| 表单状态 | 多状态反馈（默认/加载/成功） |
| 手风琴组件 | Help.vue中的FAQ展开收起 |

---

## 十、扩展建议

### 10.1 短期优化

- [ ] 添加TypeScript支持（提升代码类型安全）
- [ ] 集成Pinia状态管理（跨组件共享状态）
- [ ] 添加单元测试（Vitest + Vue Test Utils）
- [ ] 优化图片资源（WebP格式 + 懒加载）

### 10.2 中期功能

- [ ] 添加博客/新闻模块
- [ ] 实现多语言切换（i18n）
- [ ] 添加用户登录/注册功能
- [ ] 集成CMS内容管理系统

### 10.3 长期规划

- [ ] SSR服务端渲染（Nuxt.js）
- [ ] PWA渐进式应用支持
- [ ] 性能监控与埋点
- [ ] SEO优化

---

## 十一、总结

本项目是一个完整的 **中型Vue 3 SPA企业官网**，展示了现代前端开发的最佳实践：

- ✅ **完整的路由系统** - 11个页面，SPA体验流畅
- ✅ **统一的设计语言** - 每个服务页面有独立主题色
- ✅ **丰富的交互功能** - 卡片展开、Tab切换、数字动画、滚动监听
- ✅ **代码复用性高** - 相似的页面结构，不同的个性化内容
- ✅ **用户体验良好** - 页面过渡动画、hover效果、表单状态反馈
- ✅ **移动端适配** - 响应式设计，适配多种屏幕尺寸
- ✅ **无障碍支持** - prefers-reduced-motion媒体查询

项目结构清晰，代码规范，易于维护和扩展，适合作为企业级Web应用的开发模板。

---

*本文档由AI辅助生成，如有问题请联系项目负责人。*
